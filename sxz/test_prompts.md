# 测试调通功能提示词模板

## 一、功能开发类

### 数据同步模块
```
请帮我实现将聊天记录同步到明道云的功能，确保所有患者的陪诊聊天记录能够安全存储在明道云平台。
```

```
请帮我实现从明道云获取聊天记录的功能，支持在应用中查看和管理已同步的聊天记录。
```

### 患者管理模块
```
请帮我实现患者档案信息的扩展功能，需要在AddPatient.tsx和PatientDetail.tsx中添加过敏史、病史、联系方式等字段，确保患者档案包含完整的医疗信息。
```

```
请帮我将PatientList.tsx中的mock患者数据改为使用localStorage持久化存储，确保页面刷新后数据不会丢失。
```

### 陪诊流程模块
```
请帮我实现诊中记录功能，在ConsultationRecord.tsx中添加OCR识别、语音备忘录、手动补充等功能，支持记录检查项目、诊断结果、治疗建议等诊中信息。
```

```
请帮我实现诊后报告生成功能，在FinalReport.tsx中自动整合患者信息、诊前回顾、诊中详情，生成完整的专业报告。
```

### AI功能模块
```
请帮我实现AI语音转写功能，在PreConsultation.tsx中集成语音转写API，支持录制通话并将语音转换为文字。
```

```
请帮我实现OCR识别功能，在ConsultationRecord.tsx中集成OCR识别API，支持上传图片并识别病历和检查报告内容。
```

## 二、代码优化类

```
请帮我实现全局状态管理，将组件内状态改为使用Context API或Redux管理，提高组件间数据共享效率。
```

```
请帮我封装API请求，在src/app/services/api.ts中统一处理API调用、错误和加载状态，提高代码复用性。
```

```
请帮我实现组件懒加载，在App.tsx中对页面组件进行按需加载，提高初始加载速度。
```

## 三、测试验证类

```
请帮我为PatientList组件编写单元测试，确保患者列表的增删改查功能正常工作，测试覆盖率达到70%以上。
```

```
请帮我测试完整的陪诊流程，从选择患者开始，经过诊前准备、诊中记录到诊后报告生成，确保整个流程顺畅无错误。
```

```
请帮我进行性能测试，优化应用加载时间和响应速度，确保应用加载时间在2秒以内。
```

## 四、部署上线类

```
请帮我优化Vite构建配置，生成生产版本，确保资源文件得到有效优化。
```

```
请帮我添加环境变量配置，区分开发和生产环境，确保API地址等配置能够根据环境自动切换。
```

## 五、问题修复类

```
请帮我修复PatientDetail.tsx中的数据显示错误，确保患者过敏史和病史能够正确显示。
```

```
请帮我修复ConsultationForm.tsx中的表单验证问题，确保必填字段能够正确验证并提示用户。
```

```
请帮我修复localStorage存储的数据格式问题，确保患者数据能够正确序列化和反序列化。
```

```
请帮我修复聊天记录未同步到明道云的问题，确保所有聊天记录能够正常上传和存储。
```

## 使用说明
您可以根据实际需求，从上述模板中选择合适的提示词，添加具体的细节和要求，以便更准确地理解并完成开发任务。