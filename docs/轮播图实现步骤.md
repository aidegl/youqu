# 轮播图功能实现步骤

## 📋 已完成

✅ HAP 工作表结构已确认：
- 工作表 ID: `69b5dc147e3c8fc03e16dcab`
- 字段映射:
  - `69c4eb1c867350d552faf075` → 图片 (Attachment)
  - `69b5dc145c2066509f21fd24` → 链接类型 (SingleSelect)
  - `69b5dc145c2066509f21fd25` → 链接目标 (Text)
  - `69b5dc145c2066509f21fd26` → 排序 (Number)
  - `69b5dc145c2066509f21fd27` → 是否启用 (SingleSelect)

✅ API 已封装：
- `getBanners()` 函数已在 `utils/api.js` 中实现

✅ 前端已配置：
- `index.wxml` 轮播图模板已存在
- `index.js` `loadBanners()` 已调用

## 📝 下一步

1. 在 HAP 中添加轮播图数据
2. 测试轮播图显示
3. 添加点击跳转功能
