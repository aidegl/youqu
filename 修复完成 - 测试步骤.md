# ✅ 修复完成 - 测试步骤

**修复内容**: 将字段名称改为字段 ID  
**修复时间**: 2026-03-26 18:20

---

## 🧪 测试步骤

### 步骤 1: 编译项目

1. 打开微信开发者工具
2. 点击「编译」按钮
3. 等待编译完成

### 步骤 2: 打开调试器

1. 点击顶部菜单「调试器」
2. 选择「Console」面板
3. 清空之前的日志（点击🚫图标）

### 步骤 3: 查看日志

页面会自动加载，查看 Console 输出：

**✅ 成功的日志**:
```
=== [轮播图] 开始加载 ===
[API.getBanners] 开始请求 HAP 轮播图数据...
[API.getBanners] rows 数量：2
[API.getBanners] ✅ 处理完成，轮播图数量：2
[API.getBanners] 轮播图数据：[
  {id:"...", image:"https://...", link_type:"", link_target:""},
  {id:"...", image:"https://...", link_type:"", link_target:""}
]
[轮播图] ✅ 加载成功，数据：[...]
[轮播图 1] {id: "...", image: "https://..."}
[轮播图 2] {id: "...", image: "https://..."}
=== [轮播图] 加载结束 ===
```

**❌ 仍然无数据的日志**:
```
[API.getBanners] rows 数量：0
[轮播图] ❌ 加载失败或无数据
```

### 步骤 4: 手动测试（可选）

在 Console 中运行：
```javascript
const api = require('./utils/api.js');
api.getBanners().then(res => {
  console.log('=== 测试结果 ===');
  console.log('成功:', res.success);
  console.log('数量:', res.data.length);
  console.log('数据:', res.data);
});
```

---

## 🎯 预期结果

### API 返回数据

应该看到 2 条轮播图：
```javascript
[
  {
    id: "7dbff7ea-75db-4621-bc97-f97d8e093a8d",
    image: "https://p1.mingdaoyun.cn/.../dM0AcCete9eUbxdl...png",
    link_type: "",
    link_target: ""
  },
  {
    id: "7d66166f-2f99-465c-981c-6178c42e758b",
    image: "https://p1.mingdaoyun.cn/.../cdaYcF52fc228J7P...png",
    link_type: "",
    link_target: ""
  }
]
```

### 模拟器显示

- ✅ 首页显示轮播图区域
- ✅ 显示 2 张图片
- ✅ 有指示点
- ✅ 自动播放

---

## 🔍 如果还是无数据

### 检查 1: 确认 HAP 中有数据

在 Console 运行：
```javascript
const api = require('./utils/api.js');
api.getRows(api.WORKSHEET_ID.banners, {pageSize: 10}).then(res => {
  console.log('所有记录:', res.data?.rows?.length);
  if (res.data?.rows) {
    res.data.rows.forEach(r => {
      console.log('记录:', r['69b5dc145c2066509f21fd23'], 
                  '是否启用:', r['69b5dc145c2066509f21fd27']);
    });
  }
});
```

### 检查 2: 确认字段 ID 正确

查看 `utils/api.js` 中的字段 ID：
```javascript
// 是否启用字段 ID
field: '69b5dc145c2066509f21fd27'

// 图片字段 ID
row['69c4eb1c867350d552faf075']
```

### 检查 3: 重新编译

有时候代码没生效：
1. 点击「清缓存」→「清除全部」
2. 重新编译项目
3. 再次测试

---

## 📞 需要帮助？

把 Console 的完整日志截图发给我，包括：
- `[API.getBanners]` 开头的所有日志
- `[轮播图]` 开头的所有日志

---

**修改的文件**:
- `utils/api.js` - getBanners() 函数

**字段 ID 映射**:
| 字段名 | 字段 ID |
|--------|--------|
| 是否启用 | `69b5dc145c2066509f21fd27` |
| 排序 | `69b5dc145c2066509f21fd26` |
| 图片 | `69c4eb1c867350d552faf075` |
| 链接类型 | `69b5dc145c2066509f21fd24` |
| 链接目标 | `69b5dc145c2066509f21fd25` |
